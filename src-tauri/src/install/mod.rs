//! 应用升级链路的实现层。
//!
//! 与 `platform/`（把网关接进客户端）是两条独立的线：这里只负责
//! 「拿安装包 → 校验 → 静默装」，**绝不写任何配置**。
//!
//! 分层（每个文件只做一件事，便于单独测试）：
//! - `sources`   解析下载源 + 自洽校验（信任边界，坏数据硬失败）
//! - `diagnose`  本机安装形态诊断与锚定（本地探测，失败只降级）
//! - `download`  流式下载（`.part` 原子改名、缓存命中）
//! - `verify`    SHA-256 校验
//! - `installers` 按安装器类型生成静默命令并等待退出
//! - `elevate`   UAC 提权（仅 Windows）

pub mod sources;
